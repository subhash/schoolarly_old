page.close_dialog
if active_controller == 'events'
  page<<"jQuery('#calendar').fullCalendar( 'refetchEvents' );"
else
  page.replace_object @exam, :partial => 'exams/exam', :locals =>  {:exams => @exam.klass.current_exams}
end