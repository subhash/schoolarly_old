<tr id="exam-<%=exam.id%>">
	<td>
    	<%=link_to exam.subject.name, exam%>
    </td>
	<td>
    	<%=!exam.exam_date.nil? ? exam.exam_date.strftime('%d-%b-%y') : exam.exam_date%>
    </td>
    <td>
    	<%=!exam.start_time.nil? ? exam.start_time.strftime('%I:%M %p') : exam.start_time%>
    </td>
    <td>
    	<%=!exam.end_time.nil? ? exam.end_time.strftime('%I:%M %p') : exam.end_time%>
    </td>
	<td>
    	<%=exam.venue%>
    </td>
	<td>
    	<%=exam.max_score%>
    </td>
	<td>
    	<%=exam.pass_score%>
    </td>
	<td>
    	<%=link_to exam.teacher.name, exam.teacher if !exam.teacher.nil?%>
    </td>
	<%if entity.class == Student%>
		<td>
			<%=exam.score_of(entity) || '<i>Unavailable</i>'%>
		</td>
	<%end%>
	<td>
    	<%=link_to 'View/Edit Scores', exam, :title => 'View/Edit Scores'%>
    </td>
	<td>
		<%= link_to_remote 'Edit', :url =>  {:controller => :exams, :action => 'edit', :id => exam, :entity_class => entity.class.name, :entity_id => entity}, :method => :get, :html => {:title => 'Edit'}%>
   	</td>
	<%if exam.is_destroyable? %>
		<td>
			<%= link_to_remote 'Delete', :url => {:controller => :exams, :action => 'destroy', :id => exam, :entity_class => entity.class.name, :entity_id => entity}, :method => :delete, :html => {:title => 'Delete'}, :confirm => 'Are you sure?' %>
		</td>
	<%end%>
</tr>