jQuery(".jquery-dialog.open-dialog").html("<%= escape_javascript(render :text => '<blockquote>Exams could not be created. Try again.</blockquote>') %>");
jQuery(".jquery-dialog.open-dialog").append("<%= escape_javascript( render :partial => 'exam_groups/new', :locals => {:exam_group => ExamGroup.new(), :subjects => @klass.subjects, :klass => @klass, :exam_types => ExamType.all}) %>");
