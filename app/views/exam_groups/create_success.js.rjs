page.close_dialog
page.insert_object(:bottom, @exam_group, :partial => 'exam_groups/exam_group', :locals => {:options => get_show_options(@klass), :klass => @klass})
page.open_tab(Exam.new()) #TODO Have to try to change the parameter or change open_tab()