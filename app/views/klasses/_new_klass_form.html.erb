<% semantic_remote_form_for klass, :url => {:controller => :klasses, :action => 'create', :school_id => school} do |f| %>
<%= f.error_messages %>
	<%= f.input :level, :as => :select, :collection => levels %>
	<%= f.input :division%>
	<%= f.commit_button :label => 'Create' %>
<% end %>

