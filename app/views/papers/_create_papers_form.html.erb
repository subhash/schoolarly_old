<% form_remote_tag  :url => {:controller => 'papers', :action => 'create', :id => klass} do |f| %>
<%= collection_multiple_select(:klass, 'subject_ids', subjects, :id, :name, {:outer_class => 'multiple_select', :inner_class => 'multiple_select_checkbox'}) %>
<%= submit_tag "Save" %>
<%end %>