<% form_remote_tag  :url => {:controller => 'papers', :action => 'create', :id => klass}, :before => "Element.show('spinner');", :complete => "Element.hide('spinner');" do |f| %>
<%= select_tag  'school_subject_ids', options_from_collection_for_select(school_subjects, :id, :name, klass.school_subject_ids), :multiple => true, :class => "multiselect" %>
<%= submit_tag "Save" %><%= image_tag 'spinner.gif', :id=>'spinner', :style=>"display:none;" %>
<%end %>