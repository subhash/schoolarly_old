<%msg_text = params[:message][:body].blank? ? 'Message does not have a body.' : 'Message could not be sent. Try again.'%>
jQuery(".jquery-dialog.open-dialog").html("<%= escape_javascript(render :text => '<blockquote>' + msg_text + '</blockquote>') %>");
jQuery(".jquery-dialog.open-dialog").append("<%= escape_javascript(render :partial => 'conversations/new_form', :locals => {:users => users=User.find(params[:users]), :mail => Mail.new()})%>");