page << "closeDialogs();"
page << "jQuery('.tabs').tabs('select','#subjects-tab')"
page.replace_html 'subjects-tab', :partial => 'teacher_subject_allotments/teacher_subject_allotments', :object => @teacher.current_subject_allotments
page.replace_html 'add-remove-subjects-dialog', :partial => 'subjects/add_subjects_form', :locals => {:entity => @teacher, :subjects => Subject.find(:all) , :disabled => @teacher.allotted_subject_ids}
