
if @event
    page.close_dialog
    page<<"jQuery('#calendar').fullCalendar( 'refetchEvents' );"

end

if @event_series
    page.close_dialog
    page<<"jQuery('#calendar').fullCalendar( 'refetchEvents' );"
end
