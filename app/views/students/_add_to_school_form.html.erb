<% form_remote_tag :url => {:controller => :students, :action => 'add_to_school', :id => @student} do |f| %>
<%= select('student', 'school_id', schools.collect{|s|[s.name,s.id]}) %>
<%= submit_tag "Add" %>
<%end %>