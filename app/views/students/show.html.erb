<% render_tabs do |t| %>
    <% t.tab :title => 'Home', :partial => '/home', :locals => {:user => @student.user}  %>
	<% if @klass%>
		<% t.papers_tab :object => @student.papers %>
		<% t.exams_tab :locals => {:student => @student}  %>
	<%end%>
<% end %>

