page << "closeDialogs();"
page << "jQuery('.tabs').tabs('select','#subjects-tab')"
page.replace_html 'subjects-tab', :partial => 'subjects/subjects', :object => @student.subjects, :locals => {:klass => @klass, :teacher_subject_allotments => @teacher_subject_allotments, :entity => @student}
page.replace_html 'add-remove-subjects-dialog', :partial => 'subjects/add_subjects_form', :locals => {:entity => @student, :subjects => @klass.subjects , :disabled => []}
page.replace_html 'exams-tab', :partial => 'exam_groups/exam_groups', :object => @student.exams.collect{|exam| exam.exam_group}.uniq, :locals => {:options => get_show_options(@student), get_symbol_for_local(@student) => @student}

