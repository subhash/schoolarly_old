<% form_remote_tag  :url => {:controller => entity.class.name.pluralize, :action => 'add_students', :id => entity} do |f| %>
<%= select_tag  'student_ids', options_from_collection_for_select(students, :id, :name, entity.student_ids), :multiple => true, :class => "multiselect" %>
<%= submit_tag 'Save' %>
<%end %>