<% form_remote_tag  :url => {:controller => entity.class.name.pluralize, :action => 'add_students', :id => entity} do |f| %>
<%= collection_multiple_select(entity.class.name.underscore, 'student_ids', students, :id, :name, {:outer_class => 'multiple_select', :inner_class => 'multiple_select_checkbox'}) %>
<%= submit_tag "Save" %>
<%end %>
