<% form_for @student, :url => {:controller => :students, :action => 'profile_update', :id=>@student } do |student_form| %>
<%= student_form.error_messages %>
<div class="c38l">
    <table>
        <% fields_for @user_profile do |profile_form| %>
        <%= render :partial => "/profile", :object => profile_form %>
        <% end %>
    </table>
</div>
<div class="c38r">
    <table>
        <tr>
        </tr>
        <tr>
        </tr>
        <tr>
        </tr>
        <tr>
        </tr>
    </table>
    <!--TODO Check with Subhash if the partial _form can be used here... Right now using another partial.
    <% fields_for @user do |user_form| %>
    <%= render :partial => "/users/form", :object => user_form  %>
    <%end%> !-->
    <% fields_for @user do |user_form| %>
    <%= user_form.error_messages %>
    <table>
        <%= render :partial => "/user", :object => user_form %>
    </table>
    <%end %>
    <p class="icaption_right">
        <br/>
        <%= link_to_function( 'Submit' , "document.forms['edit_student_#{@student.id.to_s}'].submit()") %>|<%= link_to 'Back', {:controller => :students, :action => 'profile_edit', :id=>@student} %>
        <% end %>
    </p>
</div>
