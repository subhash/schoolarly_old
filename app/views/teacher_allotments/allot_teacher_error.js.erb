jQuery(".jquery-dialog.open-dialog").html("<%= escape_javascript(if @klass.teachers(Subject.find(params[:subject_id])).empty? then render :text => '<blockquote>Allotment could not be done as no teacher teaches the subject. <br />Try again after assigning the subject to a teacher.</blockquote>' else render :text => '<blockquote>Allotment could not be done. Try again.</blockquote>' end )%>");
jQuery(".jquery-dialog.open-dialog").append("<%= escape_javascript(render :partial => 'teacher_allotments/allot_teacher_form', :locals => {:teacher_klass_allotment => @teacher_klass_allotment, :teachers => @klass.teachers(Subject.find(params[:subject_id])), :subject => Subject.find(params[:subject_id]), :entity_klass => entity_klass}) %>");