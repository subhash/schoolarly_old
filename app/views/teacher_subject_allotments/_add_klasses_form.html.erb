<% form_remote_tag  :url => {:action => 'add_klasses', :id => teacher_subject_allotment} do |f| %>
<%= collection_multiple_select('teacher_subject_allotment', 'klass_ids', klasses, :id, :name, {:outer_class => 'multiple_select', :inner_class => 'multiple_select_checkbox', :disabled => []}) %>
<%= submit_tag "Save" %>
<%end %>