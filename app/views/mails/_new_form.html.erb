<% form_remote_tag :url => {:controller => :mails, :action => :create, :user => local_assigns[:user], :mail => local_assigns[:mail] } do  %>
<%= flash.now[:notice]%>
		<table>
			<tr>
				<th>
					To
            	</th>
	            <td>
					<%= collection_multiple_select(nil, 'user_ids', current_user.person.school.users, :id, :name, {:outer_class => 'multiple_select', :inner_class => 'multiple_select_checkbox', :selected_items => [local_assigns[:user] ? local_assigns[:user].id : nil], :disabled => [local_assigns[:user] ? local_assigns[:user].id : nil] })%>
	        	</td>
			</tr>
			<tr>
				<th>
					<%= label :subject, 'Subject' %>
            	</th>
				<%subject = 'RE:' + mail.message.subject if local_assigns[:mail]%>
	            <td>
	                <%= text_field_tag :subject, subject%>
	            </td>
			</tr>
			<tr>
            	<td colspan=2>
                	<%= text_area_tag :body, nil, :size => "33x12"%>
            	</td>
        	</tr>
		</table>
		<div>
			<%= submit_tag 'Post' %>
		</div>
	<%end %>
