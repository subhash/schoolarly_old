page.close_dialog
#Hack to replace the tab only if it exists
page.select('#messages-tab').each{|e| page.replace_tab Message, :partial => 'mails/mailboxes', :object => ['inbox', 'sentbox', 'trash']}
page.call 'initPanes'