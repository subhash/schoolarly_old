page.close_dialog
#Hack to replace the tab only if it exists
page.select('#messages-tab-section').each{|e| page.replace_tab Message, :show => false, :partial => 'mails/mailboxes', :object => ['inbox', 'sentbox', 'trash']}
page.replace_html('notice', :text => flash[:notice])
page.call 'initPanes'